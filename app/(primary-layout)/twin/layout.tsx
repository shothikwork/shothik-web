export const metadata = {
  title: "Twin — Shothik AI",
  description: "Your unified AI twin — personal AI persona and external agent identity in one.",
};

export default function TwinLayout({ children }: { children: React.ReactNode }) {
  return children;
}
