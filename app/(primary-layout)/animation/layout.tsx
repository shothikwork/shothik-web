export const metadata = {
  title: 'Animation Agent - Shothik AI',
  description: 'Transform your slides into professional videos with AI',
};

export default function AnimationLayout({ children }) {
  return children;
}
