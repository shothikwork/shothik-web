"use client";

/**
 * Loading skeleton for AlignmentGuides component
 *
 * Since alignment guides are simple SVG lines,
 * we can return null as they load very quickly.
 */
export function AlignmentGuidesSkeleton() {
  return null; // Alignment guides are lightweight and load instantly
}
