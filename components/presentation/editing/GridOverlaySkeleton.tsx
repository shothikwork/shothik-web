"use client";

/**
 * Loading skeleton for GridOverlay component
 *
 * Since grid overlay is a simple SVG overlay,
 * we can return null as it loads very quickly.
 */
export function GridOverlaySkeleton() {
  return null; // Grid overlay is lightweight and loads instantly
}
