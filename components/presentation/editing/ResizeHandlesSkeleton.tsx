"use client";

/**
 * Loading skeleton for ResizeHandles component
 *
 * Since resize handles are invisible UI elements (just handles),
 * we can return null as they load very quickly.
 */
export function ResizeHandlesSkeleton() {
  return null; // Resize handles are lightweight and load instantly
}
